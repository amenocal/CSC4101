Alejandro Menocal


Eqv? can only check for strings and numbers, not list.

even? and odd? call themselves recursively so I wasn’t sure about this, but I did it “counting down to 0” and was the only way I could think of.

=, <, >, <=, >=, they all use function that call each other too, like zero? positive? negative?
